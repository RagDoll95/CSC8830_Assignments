"""One module per assignment, each exposing a Flask Blueprint named `bp`.

app/app.py imports whatever app/assignments.py names here, so adding week 3
means adding a file beside module2.py and one registry entry -- no edits to
the shell.
"""
